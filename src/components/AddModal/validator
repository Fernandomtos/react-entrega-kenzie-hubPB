import * as yup from "yup";

export const schema = yup
  .object({
    title: yup.string().required("Nome da tecnologia é obrigatório!"),
    status: yup.string().required("Status do módulo obrigatório!"),
  })
  .required();
